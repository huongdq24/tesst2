{
  "entities": {
    "User": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "User",
      "type": "object",
      "description": "Stores user profile information, authentication-related flags, user role, and personal API keys for Bring-Your-Own-Key (BYOK) functionality. Passwords and other authentication system data are managed externally.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the User entity."
        },
        "email": {
          "type": "string",
          "description": "The user's email address.",
          "format": "email"
        },
        "displayName": {
          "type": "string",
          "description": "The user's preferred display name or full name."
        },
        "photoUrl": {
          "type": "string",
          "description": "URL to the user's profile picture.",
          "format": "uri"
        },
        "role": {
          "type": "string",
          "description": "The assigned role of the user (e.g., 'Admin', 'User')."
        },
        "hasClaimedCredit": {
          "type": "boolean",
          "description": "Indicates whether the user has claimed the $300 Google Cloud credit offer."
        },
        "geminiApiKey": {
          "type": "string",
          "description": "The user's personal API key for the Gemini AI service, enabling Bring-Your-Own-Key (BYOK) functionality."
        },
        "elevenLabsApiKey": {
          "type": "string",
          "description": "The user's personal API key for the ElevenLabs text-to-speech service, enabling BYOK functionality."
        },
        "heyGenApiKey": {
          "type": "string",
          "description": "The user's personal API key for the HeyGen video generation service, enabling BYOK functionality."
        },
        "createdAt": {
          "type": "string",
          "description": "Timestamp when the user account was created.",
          "format": "date-time"
        },
        "updatedAt": {
          "type": "string",
          "description": "Timestamp when the user profile was last updated.",
          "format": "date-time"
        }
      },
      "required": [
        "id",
        "email",
        "role",
        "hasClaimedCredit",
        "createdAt"
      ]
    },
    "File": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "File",
      "type": "object",
      "description": "Stores metadata for files uploaded by users for AI processing, including ownership, training data status, and an expiration timestamp for automatic deletion using Firestore TTL policies.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the File entity."
        },
        "ownerId": {
          "type": "string",
          "description": "Reference to the User who uploaded and owns this file. (Relationship: User 1:N File)"
        },
        "fileName": {
          "type": "string",
          "description": "The original name of the uploaded file."
        },
        "fileUrl": {
          "type": "string",
          "description": "The public URL where the file is stored (e.g., Firebase Storage).",
          "format": "uri"
        },
        "fileType": {
          "type": "string",
          "description": "The MIME type of the uploaded file (e.g., 'image/jpeg', 'audio/mp3')."
        },
        "fileSize": {
          "type": "number",
          "description": "The size of the file in bytes."
        },
        "isTrainingData": {
          "type": "boolean",
          "description": "Flag indicating if this file is designated for permanent AI model training. If false, it's subject to TTL deletion."
        },
        "expiresAt": {
          "type": "string",
          "description": "Timestamp indicating when the file metadata (and associated file in storage) should be considered for deletion by a TTL policy. Only set if 'isTrainingData' is false.",
          "format": "date-time"
        },
        "createdAt": {
          "type": "string",
          "description": "Timestamp when the file was uploaded.",
          "format": "date-time"
        }
      },
      "required": [
        "id",
        "ownerId",
        "fileName",
        "fileUrl",
        "fileType",
        "isTrainingData",
        "createdAt"
      ]
    },
    "GeneratedImage": {
      "title": "Generated Image",
      "type": "object",
      "description": "Stores metadata for images generated by the user via AI.",
      "properties": {
        "ownerId": {
          "type": "string",
          "description": "Reference to the User who owns this image."
        },
        "prompt": {
          "type": "string",
          "description": "The prompt used to generate this image."
        },
        "imageUrl": {
          "type": "string",
          "description": "The public URL where the image is stored.",
          "format": "uri"
        },
        "createdAt": {
          "type": "string",
          "description": "Timestamp when the image was generated.",
          "format": "date-time"
        }
      },
      "required": [
        "ownerId",
        "imageUrl",
        "createdAt"
      ]
    },
    "InputImage": {
      "title": "Input Image",
      "type": "object",
      "description": "Stores metadata for images uploaded by the user as input.",
      "properties": {
        "ownerId": {
          "type": "string",
          "description": "Reference to the User who owns this image."
        },
        "imageUrl": {
          "type": "string",
          "description": "The public URL where the image is stored.",
          "format": "uri"
        },
        "createdAt": {
          "type": "string",
          "description": "Timestamp when the image was uploaded.",
          "format": "date-time"
        }
      },
      "required": [
        "ownerId",
        "imageUrl",
        "createdAt"
      ]
    }
  },
  "auth": {
    "providers": [
      "password",
      "google.com",
      "phone"
    ]
  },
  "firestore": {
    "structure": [
      {
        "path": "/users/{userId}",
        "definition": {
          "entityName": "User",
          "schema": {
            "$ref": "#/backend/entities/User"
          },
          "description": "Stores individual user profiles, authentication-related flags, user role (for application logic/UI display), and personal API keys. Access is strictly limited to the document owner. Includes denormalized 'id' field matching 'userId' for authorization independence.",
          "params": [
            {
              "name": "userId",
              "description": "The unique identifier of the user, corresponding to Firebase Authentication UID."
            }
          ]
        }
      },
      {
        "path": "/files/{fileId}",
        "definition": {
          "entityName": "File",
          "schema": {
            "$ref": "#/backend/entities/File"
          },
          "description": "Stores metadata for user-uploaded files, including ownership and expiration data. Each document includes an 'ownerId' field explicitly denormalized for authorization independence. Files flagged as 'isTrainingData: false' will have 'expiresAt' set for Firestore TTL deletion.",
          "params": [
            {
              "name": "fileId",
              "description": "The unique identifier for the uploaded file."
            }
          ]
        }
      },
      {
        "path": "/generatedImages/{imageId}",
        "definition": {
          "entityName": "GeneratedImage",
          "schema": {
            "$ref": "#/backend/entities/GeneratedImage"
          },
          "description": "Stores metadata for images generated by the user via AI.",
          "params": [
            {
              "name": "imageId",
              "description": "The unique identifier for the generated image."
            }
          ]
        }
      },
      {
        "path": "/inputImages/{imageId}",
        "definition": {
          "entityName": "InputImage",
          "schema": {
            "$ref": "#/backend/entities/InputImage"
          },
          "description": "Stores metadata for images uploaded by the user as input for AI processes.",
          "params": [
            {
              "name": "imageId",
              "description": "The unique identifier for the input image."
            }
          ]
        }
      }
    ],
    "reasoning": "The Firestore structure is designed following the core principles of Authorization Independence, Structural Segregation, and consistent Access Modeling to ensure security, scalability, and debuggability. \n\n1.  **User Profiles (`/users/{userId}`)**: Each user's profile information, including their API keys, credit claim status, and role, is stored in a document `userId` within the top-level `users` collection. The `userId` directly corresponds to `request.auth.uid`, and the `role` field within this document is the single source of truth for admin privileges. This makes authorization for a user's *own* profile atomic and independent. Security rules can check `request.auth.uid == userId` without any cross-document `get()` operations for ownership-based rules.\n\n2.  **User Files (`/files/{fileId}`)**: Files uploaded by users are stored in a top-level `files` collection. Each `File` document explicitly includes an `ownerId` field. This `ownerId` is a denormalized attribute that directly references the user who owns the file. This is crucial for Authorization Independence because security rules can directly check `resource.data.ownerId == request.auth.uid` to grant read/write access. This eliminates the need for `get()` calls to a parent `User` document and supports secure list queries."
  }
}
